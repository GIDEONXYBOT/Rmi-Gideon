# Copy this file to .env and fill in your Atlas password
# Do NOT commit the real .env file

# MongoDB Atlas connection (replace <db_password> with the real password)
MONGO_URI=mongodb+srv://rmi_admin:<db_password>@rmi-teller-report.fphrmaw.mongodb.net/rmi?retryWrites=true&w=majority&appName=rmi-teller-report

# Environment
NODE_ENV=development

# Optional scheduler time (Asia/Manila HH:mm)
# RESET_TIME=00:00
